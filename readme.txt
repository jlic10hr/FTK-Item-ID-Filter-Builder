Place .txt document with item IDs in same directory as the script. Needs to be named "item_ids.txt" and contain one ID per line

Script will output an XML titled "generated_filter.xml" which can be imported into FTK